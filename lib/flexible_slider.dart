library flexible_slider;

import 'package:flutter/material.dart';

class FlexibleSlider extends StatelessWidget {
  const FlexibleSlider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
